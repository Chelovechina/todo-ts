import { FC } from "react";
import { Typography } from "@mui/material";

const Error: FC = () => {
  return <Typography color='error' variant='h2'>Что-то пошло не так</Typography>
}

export default Error;